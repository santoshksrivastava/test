import { DynamicTabDirective } from './dynamic-tab.directive';

describe('DynamicTabDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTabDirective();
    expect(directive).toBeTruthy();
  });
});
